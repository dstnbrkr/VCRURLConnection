target :ios do
  link_with 'Example-iOS', 'Tests-iOS'
  platform :ios
  podspec :path => "VCRURLConnection.podspec"
end

target :osx do
  link_with 'Tests-OSX'
  platform :osx
  podspec :path => "VCRURLConnection.podspec"
end
